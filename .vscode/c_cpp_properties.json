{
  "version": 4,
  "configurations": [
    {
      "name": "cheatah-space (C++20)",
      "compilerPath": "/usr/bin/g++",
      "cStandard": "c17",
      "cppStandard": "c++20",
      "intelliSenseMode": "linux-gcc-x64",

      "//": "Primary source of truth when present: the CMake compile database.",
      "compileCommands": "${workspaceFolder}/build/compile_commands.json",

      "//includePath": "Fallback + the key to navigation: this extension's own headers AND the sibling cheatah toolchain's stdlib, so Ctrl-click on cheatah types (ndarray::basic_ndarray, builtins::Value, …) and on #include \"ndarray.hpp\" jumps into the ORIGINAL ../cheatah library.",
      "includePath": [
        "${workspaceFolder}/space",
        "${workspaceFolder}/space/**",
        "${workspaceFolder}/../cheatah/stdlib",
        "${workspaceFolder}/../cheatah/stdlib/**",
        "${workspaceFolder}/../cheatah/stdlib/builtins"
      ],
      "browse": {
        "path": [
          "${workspaceFolder}/space",
          "${workspaceFolder}/../cheatah/stdlib"
        ],
        "limitSymbolsToIncludedHeaders": true
      }
    }
  ]
}